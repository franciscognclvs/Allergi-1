<%= simple_form_for @active_problem, remote: true, method: :post do |f| %>
  <%= f.input :problem, label: false %>
  <%= f.input :user_id, :as => :hidden, :input_html => { :value => params[:id] } %>
  <%= f.submit "Add a Problem", class:"btn btn-primary" %>
<% end %>