<div class="container" align="center">
	<div class="banner">
	  <div class="card-home">
	 
	      <h1>Allergi <strong>tech skills</strong> to <strong>creative people</strong>!</h1>
	      <p>Change your life and learn to code at one of our campuses around the world</p>
        <button type="button" class="btn btn-light">Light</button>
	      
	 
	  </div>
	</div>

	<%= form_tag("medicines/search_medicine", method: "get") do %>
  		<%= label_tag(:remedio, "Procure pelo Remédio:") %>
  		<%= text_field_tag(:remedio) %>
  		<%= submit_tag("Search") %>
	<% end %>
</div>
  </div>
